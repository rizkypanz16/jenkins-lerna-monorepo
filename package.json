{
  "name": "root",
  "private": true,
  "dependencies": {},
  "devDependencies": {
    "lerna": "^7.1.4"
  },
  "scripts": {
    "start:fe": "lerna run --scope frontend --stream start:prod",
    "start:be": "lerna run --scope backend --stream start:prod",
    "start:both": "lerna run --parallel start:prod"
  }

}
